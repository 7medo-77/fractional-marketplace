# Task 01 — Project Setup & Configuration

Steps:
1. Initialize Next.js 16 + TypeScript + App Router.
2. Install Tailwind CSS and configure.
3. Install shadcn/ui and scaffold components (Card, Button, Input, Label, Table, Tabs, Badge).
4. Install additional deps:
   - `npm install recharts date-fns clsx tailwind-merge`
   - `npm install -D @types/recharts`